services:
  - type: pserv
    name: game-server
    env: python3
    buildCommand: pip install -r requirements.txt
    startCommand: python server.py --auto
    envVars:
      - key: PORT
        value: 5555
